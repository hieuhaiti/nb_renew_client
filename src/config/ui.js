export const uiConfig = Object.freeze({
  search: Object.freeze({
    headerDebounceMs: 300,
    headerResultLimit: 7,
    headerPointFetchLimit: 200,
    headerTourFetchLimit: 100,
    tourPageDebounceMs: 500,
  }),
});
